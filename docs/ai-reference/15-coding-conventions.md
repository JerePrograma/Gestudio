# Convenciones de código

> Estado: PARCIAL  
> Última revisión: 2026-07-24  
> Fuentes principales: `backend/pom.xml`, `frontend/package.json`, `.kiro/steering`

## Confirmadas

Java 21 y UTF-8; paquetes `gestudio`; Spring MVC/Security/JPA/Validation; MapStruct/Lombok; terminología mayormente española; TypeScript con `tsc -b`; ESLint; Flyway inmutable; validación sin `-SkipTests`.

## Prácticas

Conservar terminología; manejar errores centralmente; evitar exponer entidades JPA; no registrar secretos/PII; actualizar pruebas y documentación operativa.

## PENDIENTE

Extraer detalles de `.kiro/steering/PROGRAMMING_PRACTICES.md`, ESLint, TSConfig y formato real.